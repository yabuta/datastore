車のデータをデータベースに送るためのサンプルプログラム
予定としてはCANからとれるデータを送信するつもりだが、今は適当な数値データを送っている。

コンパイル
./compile.sh


使い方
./start.sh

./start.shを実行すればプログラムがスタートする。
その後、Insertのサンプルデータを送るかSelectのサンプルデータを送るか終了するかを聞かれる。

Insertは適当な数値データをCar_Dataテーブルに一秒おきに送る。標準入力から"quit"と入力すると終了する。

SelectはCar_Dataテーブルから適当な列の値を取得してすべて表示する