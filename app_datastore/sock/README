データをデータベースへ格納する。

データベースの格納にはサーバ側のプログラムにソケット通信でポート5555に
データを送信して行う。
データの形式は

数値,テーブル名(カラム)VALUES(データ);/\n


数値はinsertが1、終了が0になっている。終了メッセージは送らないとサーバが困るので送るようにする。
ほかにもテーブル作成や削除のコマンドがあるが今回はパス。そんなにさくさくテーブルを作られても困るし。

後ろにいろいろついているのは試行錯誤の結果であって/は跡で削除する予定。改行を入れないとサーバ側で正しく認識してくれない。
たぶんreadLine()を使っているからと思われる。


このプログラムはC++で書かれている。zmpのcanデータ取得プログラムに組み込み予定だから。

